# xsretrieval — general (platform-agnostic) requirements.
#
# Core numeric stack (always required):
numpy>=1.24,<3
scipy>=1.10

# Deep-learning + foundation-model stack (optional but recommended; the numpy
# retrieval/whitening path runs without these):
torch>=2.1
faiss-cpu>=1.7.4
transformers>=4.38
timm>=0.9.16
open_clip_torch>=2.24
huggingface_hub>=0.21
scikit-learn>=1.3

# Config / IO / geospatial:
pyyaml>=6.0
pillow>=10.0
rasterio>=1.3
tqdm>=4.65

# Serving / demo (optional):
fastapi>=0.110
uvicorn>=0.27
gradio>=4.0
python-multipart>=0.0.9

# Testing:
pytest>=8.0

# NOTE: for a reproducible CPU-only install (no CUDA wheels) use
#   pip install -r requirements-cpu.txt
