# xsretrieval — CPU-only, reproducible install (no CUDA wheels).
#
#   pip install -r requirements-cpu.txt
#
# Pulls CPU PyTorch wheels from the official PyTorch CPU index; everything else
# from PyPI. Designed for the zero-GPU deployment the system targets.

--extra-index-url https://download.pytorch.org/whl/cpu

# Core numeric stack.
numpy>=1.24,<3
scipy>=1.10

# PyTorch (CPU build) — projection-head training + torch-backed backbones.
torch>=2.1,<2.9

# Fast vector index (CPU build of FAISS).
faiss-cpu>=1.7.4

# Foundation models / encoders (DINOv2, OpenCLIP, RemoteCLIP, DOFA, CROMA).
transformers>=4.38
timm>=0.9.16
open_clip_torch>=2.24
huggingface_hub>=0.21

# Classical ML utilities (PCA / metrics cross-checks).
scikit-learn>=1.3

# Config / IO / geospatial raster loading.
pyyaml>=6.0
pillow>=10.0
rasterio>=1.3
tqdm>=4.65

# Serving + interactive demo.
fastapi>=0.110
uvicorn>=0.27
gradio>=4.0
python-multipart>=0.0.9

# Tests.
pytest>=8.0
